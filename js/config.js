/**
 * Configuration settings for diagram layout and node sizes.
 * @module config
 */
export const config = {
  mainStartX: 100,
  mainSpacing: 300,
  centerY: window.innerHeight / 2,
  mainRadius: 50,
  subRadius: 20,
  subSubRadius: 10
};
